import { Theme } from "../styles/theme.slint";

export component WorkspacesWidget {
    in-out property <int> active-workspace: 1;
    in-out property <int> total-workspaces: 8;
    
    Rectangle {
        vertical-stretch: 1;
        // background: Theme.background;
        // background: blue;
        border-radius: 12px;
        
        VerticalLayout {
            alignment: center;
            
            HorizontalLayout {
                alignment: center;
                

                Rectangle {
                    background: #414b459e;
                    horizontal-stretch: 1;
                    border-radius: 12px;
                    // padding: 5px;

                    HorizontalLayout {
                        alignment: center;
                        spacing: 3px;
                        padding: 2px;
                        
                        
                        for i in root.total-workspaces : Rectangle {
                            width: 20px;
                            height: 20px;
                            border-radius: 10px;
                            background: i == root.active-workspace ? #c7dfbedf : #00000000;

                            Rectangle {
                                width: 4px;
                                height: 4px;
                                border-radius: 2px;
                                background: i == root.active-workspace ? #121312 : Theme.foreground;
                                opacity: i == root.active-workspace ? 1.0 : 1.0;
                            }
                        }
                    }
                }
            }
        }
    }
}