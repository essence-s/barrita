import { Theme } from "../styles/theme.slint";

export component BatteryWidget {
    in-out property <string> percentage: "100%";
    in-out property <bool> is-charging: false;
    in-out property <string> icon: "Battery_Android_Frame_Full";
    callback clicked();
    
    Rectangle {
        vertical-stretch: 1;
        
        touch_area := TouchArea {
            clicked => { root.clicked(); }
        }
        
        VerticalLayout {
            alignment: center;
            
            hover_rect := Rectangle {
                // width: 30px;
                height: 30px;
                background: touch_area.has-hover ? #e7e8eb2f : transparent;
                border-width: 1px;
                border-color: touch_area.has-hover ? #ffffff0e : transparent;
                border-radius: 4px;
                
                VerticalLayout {
                    alignment: center;
                    padding-left: 6px;
                    padding-right: 6px;
                    
                 
                    Rectangle { 
                        
                        Rectangle {
                            x: 3px; y: 10px;
                            width:15px;
                            height: 6px;
                            background: root.is-charging ? Theme.warning : Theme.foreground-icon;
                            clip: true;
                        }
                        
                        Image {
                            source: @image-url("../../assets/icons/battery_android_0.svg");
                            width: 26px; 
                            height: 26px;
                            image-fit: contain;
                            colorize: root.is-charging ? Theme.warning : Theme.foreground-icon;
                        }
                    }
                   
                }
            }
        }
    }
}
