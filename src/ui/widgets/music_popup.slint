import { Theme } from "../styles/theme.slint";

export component MusicPopupWindow inherits Window {
    no-frame: true;
    background: Theme.background;
    always-on-top: true;
    width: 300px;
    height: 200px;
    
    Rectangle {
        Text {
            text: "Hola";
            font-size: 24px;
            color: Theme.foreground;
        }
    }
}