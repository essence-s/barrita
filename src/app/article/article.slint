import { Theme } from "../../ui/styles/theme.slint";

export global ArticleAdapter {
    callback article-clicked();
}

export component ArticleWidget {

    VerticalLayout {
        alignment: center;

        Rectangle {
            width: 28px;
            height: 28px;
            border-radius: 14px;
            border-width: 2px;
            border-color: touch_area.has-hover ? #ffffff42 : transparent;
            background: touch_area.has-hover ? #262722ed : Theme.workspaces_bg_color;

            Image {
                source: @image-url("../../assets/icons/article.svg");
                width: 18px;
                height: 18px;
                image-fit: contain;
                colorize: Theme.foreground-icon;
            }
        }
    }

    touch_area := TouchArea {
        clicked => {
            ArticleAdapter.article-clicked();
        }
    }
}
